diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 4b392bf4..54373d99 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -4,11 +4,13 @@ on:
   workflow_dispatch:
   push:
     branches: [ main, 'issue/**' ]
+  pull_request:
 
 permissions: read-all
 
 jobs:
   build-java:
+    if: ${{ github.event_name != 'pull_request' }}
     strategy:
       matrix:
         java-version: [ base, main ]
@@ -25,3 +27,18 @@ jobs:
         uses: spring-projects/spring-data-build/actions/maven-build@main
         with:
           settings-xml: '${{ vars.SETTINGS_XML }}'
+
+  build-pull-request:
+    if: ${{ github.event_name == 'pull_request' }}
+    name: Build pull request
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v6
+      - name: Setup Java and Maven
+        uses: spring-projects/spring-data-build/actions/setup-maven@main
+        with:
+          java-version: base
+      - name: Compile
+        run: ./mvnw -B -ntp -DskipTests compile
+      - name: Run LazyUnitTests
+        run: ./mvnw -B -ntp -Dtest=LazyUnitTests test
